<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.antenado.sthetosample">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".StethoSample">


        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.JsonGetActivity"/>
        <activity android:name=".activities.JsonPostActivity"/>
        <activity android:name=".activities.ImageGetActivity"/>
        <activity android:name=".activities.SharedPreferencesActivity"/>
        <activity android:name=".activities.SqliteActivity"/>
    </application>

</manifest>